Here we have Strategy design pattern. We have 1 abstract class, 1 interface and subclasses. Characters set the weapon with
method setWeapon() and use with useWeapon() method of Character super class. We can realize any weapon with WeaponBehavior
interface.